# Verify-is-logical-expression-is-correct
Program that verifies if a logical expression is correct.

This program is to check if a logical expression is correct or not:
(!A) is correct, but (!A is not.
